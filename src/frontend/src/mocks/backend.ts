import type { backendInterface } from "../backend";

// Minimal mock for visual QA — landing page does not call backend
export const mockBackend: backendInterface = {} as unknown as backendInterface;
